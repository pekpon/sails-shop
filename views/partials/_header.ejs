<div class="container">
    <span class="loginBox pull-right">
	    <% if(req.isAuthenticated()){ %>
			<a href="/account">User: <%= req.user.email %></a> | <a href="/logout">Logout</a>
		<% }else{ %>
			<a href="/login">Login</a></li>
		<% } %> | 

		<a class="dropdown">
			<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
				<span class="glyphicon glyphicon-shopping-cart"></span> My cart ({{itemsInCart}}) <span class="caret"></span>
    		</a>
    		<ul ng-show="cart.length>0" class="dropdown-menu dropdown-cart" role="menu">
        		<li ng-repeat="item in cart|limitTo:4">
            		<span class="item">
						<span class="item-left">
							<img src="{{item.product.images[0]}}" alt="" width="50"/>
							<span class="item-info">
								<span>{{item.product.name}}</span>
            					<span>{{item.qty}} x {{item.product.price}}</span>
            				</span>
            			</span>
            			<span class="item-right">
							<button class="btn btn-xs btn-danger pull-right" ng-click="removeCart($index)">
								<span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
            				</button>
            			</span>
            		</span>
        		</li>
		        <li class="divider"></li>
		        <li><a class="text-center" href="/cart/show">View Cart</a></li>
    		</ul>
		</a>
	</span>
	<h1 class="text-center">Welcome to <a href="/">Sails-Shop</a></h1>
	<p class="text-center">
	    <% for(var i = 0; i < res.menu.length; i++){ %>
	        <a href="/collections/<%= res.menu[i].slug %>">
	            <%= res.menu[i].name %>
	        </a>
	        <% if(i < res.menu.length - 1) { %>|
	            <% } %>
                <% } %>
	</p>
</div>
